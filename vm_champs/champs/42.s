
.comment        "Just #gfg a basic Winner Progra



	"
.name          "\n
" 

asd:
name:	live	%42		# entree
	ld	%0,r5 ;dfd
	ld	45,r2
	# and r1, r1, r1
	# and 5, 5, r1
	# and %5, %5, r1
	 # and :l2, %:l2, r1
	 # or r1, r1, r1
	 # or 5, 5, r1
	 # or %5, %5, r1
	 # or :l2, %:l2, r1
	 # xor r1, r1, r1
	 # xor 5, 5, r1
	 # xor %5, %5, r1
	 # xor :l2, %:l2, r1
	# zjmp %:hello
	# fork  %-6
	# fork %:hello
	# lfork  %-6
	# lfork %:hello
	# ldi r1, r1, r1
	# ldi 5, r5, r1
	# ldi %5, %5, r1
	# ldi :l2, %:l2, r1
	# lldi r1, r1, r1
	# lldi 5, r5, r1
	# lldi %5, %5, r1
	# lldi :l2, %:l2, r1
	# sti r1, r1, r1
		# sti r2, 5, r1
	# sti r2, %5, r1
	# sti r2, %:l2, r1
	# sti r1, r1, %5
	# sti r4, 5, %:hello
	# aff r1
	# lld 5, r1
	# lld %5, r4
	# lld %:hello, r5

	zjmp	%4500000000000000000000
ls:
#	 asdsadasd
